/*
var nodemon = require('gulp-nodemon');

gulp.task('server', function() {
  nodemon({
    verbose: false,
    script: 'index.js',
    watch: ['lib', 'app.js'],
    ext: 'js',
    env: {
      NODE_ENV: 'development'
    }
  });
});
*/
